<h1>All The Guests</h1>

<ul>
  <% @guests.each do |guest| %>
    <li><%= link_to(guest.name, guest_path(guest)) %></li>
  <% end %>
</ul>


<%= link_to "All episodes", episodes_path(@episodes) %>
<%= link_to "Create a new appearance", new_appearance_path(@appearance) %>